<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.cxyxh.hotspot.common.mapper.RoleMapper" >

  <resultMap id="BaseResultMap" type="org.cxyxh.hotspot.common.entity.Role" >
    <id column="role_id" property="roleId" jdbcType="INTEGER" />
    <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="nameZh" property="nameZh" jdbcType="VARCHAR" />
  </resultMap>

  <select id="getRolesByUserId" resultMap="BaseResultMap">
    SELECT
      r.*
    FROM
      role r
        LEFT JOIN user_role ur ON r.role_id = ur.role_id
        LEFT JOIN USER u ON u.user_id = ur.user_id
    WHERE
      u.user_id = #{userId};
  </select>

</mapper>